# Origin of the deployed reconciliation-api.
# Leave EMPTY for local dev (Vite proxies /api and /health to localhost:4000).
# Set in production (e.g. Vercel env vars) to the Heroku API URL, no trailing slash.
VITE_API_BASE_URL=https://valence-reconciliation-801df74a9b90.herokuapp.com
